<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:theme="@style/Messaging_menuItemStyle"
    >


        <item
            android:id="@+id/logout"
            android:title="Logout"
            app:showAsAction="never" />
        <item
            android:id="@+id/username_menu"
            android:title="Username"
            app:showAsAction="always"
            app:actionLayout="@layout/profile_username"

          />
        <item
            android:id="@+id/profile_image_menu"
            android:icon="@mipmap/ic_launcher_round"
            android:title="Profile_image"
            app:showAsAction="always"
            app:actionLayout="@layout/profile_icon_layout"
            />




</menu>